# render.yaml - Render.com deployment config (FREE tier available)
services:
  - type: web
    name: dataforge-api
    runtime: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: RATE_LIMIT_PER_MINUTE
        value: "60"
    healthCheckPath: /health
    plan: free
